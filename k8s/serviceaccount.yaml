apiVersion: v1
kind: ServiceAccount
metadata:
  name: oke-sa
  namespace: default
  annotations:
    # Optional: Add annotations for documentation
    description: "Service account for OKE pods accessing GCP resources"
